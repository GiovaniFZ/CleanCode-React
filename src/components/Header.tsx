export default function Header() {
    return (
        <header>
            <h1>My new innovate to-do list !</h1>
            <button onClick={() => { }}>Add new todo</button>
        </header>
    )
}